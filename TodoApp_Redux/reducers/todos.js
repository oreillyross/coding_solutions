const todos = (state = [], action) => {
      
      switch (action.type) {
          case 'ADD_TODO':
          case 'TOGGLE_TODO':  
          default:
              return state
      }
      
}

export default todos